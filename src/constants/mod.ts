import { config } from './config';
import { appName, binName } from './name';
import { configPath, logPath } from './path';
import { version } from './version';

export {
    appName as AppName,
    binName as BinName,
    config as Config,
    configPath as ConfigPath,
    logPath as LogPath,
    version as Version,
};
